ID: MR_GIT_APP_1
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: db
   type: hdb
   path: db
   requires:
    - name: hdi_db


 - name: PERSISTDB
   type: hdb
   path: PERSISTDB
   requires:
    - name: hdi_PERSISTDB

resources:
 - name: hdi_db
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container

 - name: hdi_PERSISTDB
   properties:
      hdi-container-name_2: ${service-name}
   type: com.sap.xs.hdi-container

